/**
 * FLOWSHIELD — useTheme
 * Re-exports the synchronized Theme Context hook.
 */

export { useTheme, ThemeProvider } from '../context/ThemeContext';
export type { ThemeMode } from '../context/ThemeContext';
